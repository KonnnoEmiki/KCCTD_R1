using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CmameraSwitch : MonoBehaviour
{
    public GameObject MCamera;
    //public GameObject SCamera;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {

       /* if (Master.MenuOn == true)
            MCamera.GetComponent<cinemachinevirtualcamera>().enable = false;
        else
            MCamera.GetComponent<cinemachinebrain>().enable = true;*/
    }
}
